# Required ONLY for the first-time PDF parsing via LlamaParse.
# Get your key at: https://cloud.llamaindex.ai/
# After the index is built (./storage exists), this key is no longer needed.
LLAMA_CLOUD_API_KEY=
